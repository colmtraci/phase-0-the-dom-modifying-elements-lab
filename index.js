// Write your code here!
main.remove(); // Remove Elements from the DOM

const newHeader = document.createElement('h1'); //Add Elements to the DOM

newHeader.id = 'victory';

newHeader.innerHTML = "Colm is the champion"; //Add Elements to the DOM via innerHTML